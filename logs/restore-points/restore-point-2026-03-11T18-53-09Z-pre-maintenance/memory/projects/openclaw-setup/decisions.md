# Decisions
- Use canonical markdown files as the memory source of truth.
- Use separate project memory folders for real projects.
- Keep both headless and visible browser setups.
- Build memory seriously rather than relying only on light hooks.
