## Active

- Canonical memory now uses a structured registry for active items instead of inferring item fields from markdown bullets.
- Canonical registry now requires explicit approval for high-importance stable items.

## Superseded

- Canonical memory now uses a structured registry for active items instead of inferring item fields from markdown bullets.
